#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio;
    cin.tie(NULL);
    cout.tie(NULL);

    int T;
    cin >> T;
    for (int i = T; i > 0; i--)
    {
        cout << i << "\n";
    }
}